
// // definir función
// // parámetros que se usarán para crear el objeto de usuario.
// function createUser(username, state, role) {
//     return {
//         // se asigna el valor de los parámetros a la propiedad del objeto de usuario.
//       username: username,
//       state: state,
//       role: role,
//       // función para cambiar el estado del usuario
//       changeState: function(newState) {
//         this.state = newState;
//       },
//       // muestra en la consola la información del usuario.
//       displayInfo: function() {
//         console.log(`Nombre de usuario: ${this.username}`);
//         console.log(`Estado: ${this.state}`);
//         console.log(`Rol: ${this.role}`);
//       }
//     };
//   }
  
//   // Crear un nuevo usuario con datos iniciales
//   const user = createUser("Paula", "Activo/a", "Jefa/e");
//    user.displayInfo();
//   user.displayInfo();